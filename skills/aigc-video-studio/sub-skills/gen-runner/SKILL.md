---
name: gen-runner
description: GenSpec→任务卡批次（UI 卡+API 卡）；回收 inbox；VLM 初筛；登记 TakeLog（C9）；组织选片。
---

# SK6 · gen-runner（生成调度与回收）

> 占位骨架（Phase 1）。完整提示词在后续阶段填充，详见设计稿 §5 SK6。

## 职责

GenSpec→任务卡批次（UI 卡+API 卡）；回收 inbox；VLM 初筛；登记 TakeLog（C9）；组织选片。

## 契约与脚本

参见设计稿 §3.2（契约）与 §5（本技能脚本职责）、§8（脚本实现规范）。本阶段已实现的相关 P0 脚本见仓库根 README 的「MVP 闭环说明」。
