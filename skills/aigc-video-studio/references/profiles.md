# 四档 Profile

> 占位骨架（Phase 1）。详见设计稿 §7.3。

- premium（精品单片）
- series（剧集量产）
- rapid（快速验证）
- exploration（探索）

render_passes 默认：premium=draft:api/720p + final:ui/1080p；series=draft:api 优先；rapid=draft:api/Story 可只一 pass；exploration=draft:api 便宜快迭代。
